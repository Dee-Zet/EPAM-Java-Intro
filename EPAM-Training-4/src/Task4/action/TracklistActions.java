package Task4.action;

import Task4.entity.SongData;

import java.util.List;

public class TracklistActions {

    /*public void add(int discNumber, int trackNumber, SongData songData) {
        List<SongData> disc = discs.get(discNumber);
        disc.add(trackNumber, songData);
    }

    public void add(int discNumber, SongData songData) {
        List<SongData> disc = discs.get(discNumber);
        disc.add(songData);
    }

    public void add(SongData songData) {
        add(discs.size() - 1, songData);
    }

    public void deleteDisk(int diskNumber) {
        discs.remove(diskNumber);
    }

    public void remove(int discNumber, int trackNumber) {
        List<SongData> disc = discs.get(discNumber);
        disc.remove(trackNumber);
    }

    public void removeLast(int discNumber) {
        List<SongData> disc = discs.get(discNumber);
        disc.remove(disc.size() - 1);
    }

    public void removeLast() {
        removeLast(discs.size() - 1);
    }

    public void sort(int disc, SortParameter sort) {

    }*/
}
